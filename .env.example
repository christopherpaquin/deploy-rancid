# .env.example
# RANCID Deployment Configuration
# 
# Copy this file to .env and customize the values for your environment.
# The .env file is sourced by deploy-rancid.sh to set default values
# before creating /etc/rancid/rancid.env
#
# IMPORTANT: Never commit .env to git (it's in .gitignore)

# RANCID Groups - Array of group names (space-separated or bash array syntax)
# Example: RANCID_GROUPS_DEFAULT=("routers" "switches" "firewalls")
# Or: RANCID_GROUPS_DEFAULT="routers switches firewalls"
RANCID_GROUPS_DEFAULT=("routers" "switches")

# Git configuration for RANCID repositories
GIT_NAME_DEFAULT="RANCID Automation"
GIT_EMAIL_DEFAULT="rancid@example.com"

# Optional: Override system paths (usually not needed)
# BASEDIR_DEFAULT="/var/lib/rancid"
# ETCDIR_DEFAULT="/etc/rancid"
# CRON_FILE_DEFAULT="/etc/cron.d/rancid"
# CRON_LINE_DEFAULT="0 0 * * * rancid /usr/bin/rancid-run"
